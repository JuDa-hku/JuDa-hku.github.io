to preview, be sure to pass an empty string to --baseurl option and you can view it localhost:4000

jekyll serve --baseurl ''


we can add files first
git add .
then commit to the local place with some notation
git commit -m 'some word'
then we push it to the remote
git push origin(the name of the remote) gh-pages(the name of the local)